# opensplatting

## 数据与说明

- **COLMAP 稀疏重建流程**（从图像到稀疏点云，OpenSplat 之前）：见 [`docs/COLMAP_图像到稀疏点云流程.md`](docs/COLMAP_图像到稀疏点云流程.md)。
- **稀疏模型**：仓库内 [`sparse/`](sparse/)（大文件通过 Git LFS 管理，克隆后需安装 [Git LFS](https://git-lfs.com/) 并执行 `git lfs pull`）。